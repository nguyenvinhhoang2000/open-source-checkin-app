export function findDirectionTooltip(data, record) {
  return data.find((item) => item.direction === record);
}

export function findArrowTooltip(data, record) {
  return data.find((item) => item.direction === record);
}
